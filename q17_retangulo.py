# Entrada
base = float(input('Digite o valor da base do retângulo: '))
altura = float(input('Digite o valor da altura do retângulo: '))

# Processamento 
area = base * altura

# Saída
print(f'A área do retângulo de base {base:.1f} e altura {altura:.1f} é igual a {area:.1f}.')
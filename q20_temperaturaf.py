# Entrada
temp_celsius = float(input('Digite uma temperatura em °C: '))

# Processamento 
temp_fahrenheit = ((9 * (temp_celsius)) + 160) / 5 

# Saída
print(f'A temperatura {temp_celsius:.1f} °C, em fahrenheit, é igual a {temp_fahrenheit:.1f} °F.')
# Entrada
base = float(input('Digite a medida da base do triângulo: '))
altura = float(input('Digite a medida da altura do triângulo: '))

# Processamento
area = (base * altura) / 2

# Saída 
print(f'A área do triângulo de base {base:.1f} e altura {altura:.1f} é igual a {area:.1f}.')